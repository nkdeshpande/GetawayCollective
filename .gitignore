node_modules/
.next/
.turbo/
out/
build/
coverage/

# Generated token package — regenerate with `npm run tokens`
dist/

*.log
npm-debug.log*
.DS_Store
Thumbs.db
.env
.env.local
.env*.local
*.tsbuildinfo
.vscode/
.idea/
~$*.xlsx

# Package-manager store and tool debris — never tracked
.pnpm-store/
*.inspect.ndjson

# Agent working directory. The scripts that BUILT the canon live here;
# the canon itself is committed under outputs/. Ignored rather than
# deleted — nothing is lost, and it can be tracked later if the
# generation scripts turn out to be worth versioning.
.codex-sheet-work/

# Scratch output from tooling runs (rendered pages, intermediate files).
# Also declared in vocab-lint's NOT_PROSE: that linter walks the
# filesystem rather than the index, so gitignoring alone leaves it
# refusing to report a PASS over a directory nobody decided about.
tmp/
